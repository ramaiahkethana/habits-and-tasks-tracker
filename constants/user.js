const user = {
  userRegistration: {
    success: {
      default: 'Successfully registered the user',
    },
    error: {
      default: 'Error while registering the user',
    },
  }
}

export default user;